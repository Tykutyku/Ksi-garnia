class Book{
constructor(title,isbn,author,date){
  tihs.title = title;
  this.isbn = isbn;
  this.author = author;
  tihs.date = date;
  }
}

const log = require("./Books.json")

console.log(log);
